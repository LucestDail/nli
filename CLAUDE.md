# CLAUDE.md — 동네살기지수(NLI)

Loop-driven work. 모든 비자명한 작업은 루프로:

**Contract → Act → Verify → Record → Repeat (or Stop)**

Bias: **신중함 > 속도.** 사소한 작업엔 판단으로.

이 프로젝트는 공공데이터 표준데이터를 읍면동 단위로 융합한 살기좋은동네 지수다.
순수 Python(pandas · DuckDB spatial · mapshaper) 데이터/지오 파이프라인 + 정적 웹 대시보드.
빌드/테스트 프레임워크가 없으므로, 검증은 **"스크립트 실행 + 실제 수치·상식 확인"** 으로 한다.

## 1. Contract First — "done"을 기계가 확인할 수 있게

코드/파이프라인을 손대기 전에 "완료"를 검증 가능한 형태로 정의한다.

- 데이터 편입 → **검증 수치**로: 건수, 좌표 유효율(위경도 범위 안), 조인 매칭률, 전국 합계 정합성(예: 총인구 ≈ 51.8M, 상가 ≈ 270만).
- 모호한 요청 → 해석안을 제시하고 **물어라.** 조용히 하나 고르지 말 것.
- 추측 금지: 실제 소스 컬럼과 코드체계를 먼저 확인한다(예: SGIS 코드 ≠ 법정동 코드 — 이름/크로스워크로 매핑). 외부 API는 문서·기억 말고 **실제 응답을 봐라**(recall은 낡는다).

> 확인할 수 없는 계약은 멈출 수 없는 루프다.

## 2. Verify — 트랜스크립트가 아니라 체커를 믿어라

"done"은 **방금 나온 실측**으로만 주장한다. 추론으로 주장하지 않는다.

- 파이프라인 표준 순서(**루트에서 실행**, CWD-상대 경로): `pipeline/build_geoframe.py` → `pipeline/score_engine.py` → `pipeline/analyze_nli.py` → `pipeline/export_map_geojson.py`(mapshaper 위상 단순화) → `pipeline/generate_points.py` → `pipeline/build_web.py` → `cp nli_map.html index.html`. 재실행 후 수치를 확인한다.
- 디렉토리: `pipeline/`(코어) · `prep/`(가공) · `collect/`(API수집) · `analysis/`(가중치·통계) · `tools/`(핸드오프) · `legacy/`(구 Selenium, 미사용) · `docs/`(기획·분석 문서). 산출물 index.html·nli_map.html은 루트.
- **상식 검증**: 결과가 지리적으로 말이 되는가(노형동에 약국 47개, 강남 S등급, 매칭률 99.9%). 안 맞으면 데이터 품질 이슈를 의심하고 파고든다(예: 폴리곤 기하중심 → 인구가중 중심점).
- **배포 검증**: 배포 후 `curl http://<홈랩서버>/nli/`로 신기능 마커 문자열이 실제 서비스되는지 확인(서버 주소는 로컬 메모리 참조).
- 체커가 빨간불이면 그게 반복하라는 신호다 — **빨간불을 말로 무마하지 마라.**

## 3. Act Small

작은 반복은 수렴하고, 큰 변경은 표류한다.

- 계약을 만족하는 **최소 변경**만. 요청 안 한 기능·추상화·설정화 금지.
- 인접 코드를 리팩터하거나 "개선"하지 말고 기존 스타일에 맞춘다.
- 내 변경이 만든 orphan은 제거하고, 기존 dead code는 건드리지 말고 언급만 한다.

## 4. Record — 상태는 컨텍스트보다 오래 산다

컨텍스트 창은 메모리가 아니다. 압축되면 사라진다.

- **메모리 시스템을 쓴다**: `~/.claude/projects/.../memory/` (MEMORY.md 인덱스 + 사실 파일). 이 프로젝트는 git 히스토리가 얕아 **매 세션 상태를 메모리로 복원**한다. 진행·결정·막힌 지점·비자명한 발견을 여기 남긴다.
- 일회성 인라인 대신 **재현 스크립트로 남긴다**: 파이프라인 각 단계가 파일로 존재해야 한다(`prep_*`, `collect_*`, `build_*`, `export_*`).
- 대용량·중간 산출물은 커밋하지 않는다(`.gitignore`). 배포엔 자체완결 `index.html` 하나면 된다.

## 5. Stop Conditions — 루프엔 출구가 필요하다

- **같은 시도 3번 실패 → 멈추고** 배운 것을 보고한 뒤 질문한다. (교훈: 외부 스로틀·쿼터는 코드로 못 뚫는다. 계속 던지지 말고 시간/대안을 택하라.)
- 계약이 검증 불가·모호로 판명되면 → 멈추고 **재계약**한다.
- **되돌리기 어렵거나 외부로 나가는 행위(서버 배포, 데이터 삭제, force-push, 외부 전송) → 항상 사람 승인.** 실행 중인 프로세스를 죽이기 전에도 손실 여부를 먼저 본다.

---

**이 규칙이 작동하는 신호:** 모든 "done"에 실측이 붙고, diff가 작게 유지되고, 루프가 헛돌지 않고 멈춘다.
