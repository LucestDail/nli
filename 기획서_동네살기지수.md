# 동네 살기 지수 (Neighborhood Livability Index, NLI) — 기획서

> 공공데이터포털 표준데이터 300종을 융합해, **읍면동 단위로 "얼마나 살기 좋은 동네인가"를 정량화**하는 지수·지도·대시보드.
> 작성 기준일: 2026-07-16 · 데이터 기준: 공공데이터포털 목록개방현황(2026-06-30)

---

## 1. 문제의식 & 가치 제안

공공데이터포털에는 96,056개의 데이터셋이 있지만 대부분 **단일 시설·단일 주제의 나열**에 그친다. "약국이 몇 개"는 정보이지만, "이 동네가 아이 키우기 좋은가"는 여러 데이터를 **엮어야** 나오는 판단이다.

NLI는 흩어진 시설·안전·환경·교통·복지 데이터를 **하나의 공통 지리 단위(읍면동)로 결합·정규화**해 다음을 제공한다.

| 사용자 | 얻는 가치 |
|---|---|
| 시민(이사·부동산) | "우리 가족에게 맞는 동네" 비교·랭킹 (페르소나별 가중치) |
| 지자체·정책 | **인프라 사각지대(blind spot)** 발굴 → 예산 배분 근거 |
| 연구·언론 | 지역 격차의 정량 지표, 재현 가능한 데이터 근거 |

**핵심 차별점**: ① 300종 표준데이터를 한 파이프라인으로 융합 ② 사용자가 가치관(페르소나)에 따라 가중치를 바꾸면 지도가 실시간 재계산 ③ 단순 총점이 아니라 "무엇이 부족한지"를 짚는 **결핍·사각지대 판정 로직**.

---

## 1.5 경쟁 환경 & 차별화

이미 공공·사설에 유사 서비스가 존재한다. 우리는 **데이터는 공공 이상, 기능은 사설 이상, 디자인은 둘 다 이상**을 목표로 한다.

| | **SGIS 살고싶은 우리동네** (공공/국가데이터처) | **동네스코어** (사설) | **NLI (우리)** |
|---|---|---|---|
| 공간 해상도 | 시군구/읍면동 **통계 집계** | **시군구 230개** | **읍면동 3,500+ → 격자** |
| 지표 | 7카테고리 **51지표** (비율·통계) | 핵심 4소스 + 9테마 | **51 커버 + 확장** |
| 데이터 성격 | 통계청 집계값 | 4개 기관, 270만 포인트 | **300종 개별시설 좌표** |
| 접근성 | 개수·비율만 | 개수·비율만 | **최근접 도보시간(체감)** |
| 산출 투명성 | 방법론 접근난 | **비공개** | **완전 공개·재현가능** |
| 정책 활용 | 추천 위주 | 추천 위주 | **사각지대·결핍 판정** |

**두 서비스의 공통 빈틈** = "읍면동 해상도 × 실제 개별시설 접근성"을 동시에 못 함 → **우리의 자리**.

### 6가지 전략적 강점
1. **공간 해상도**: 읍면동 단위 — "같은 구라도 동마다 다르다" (동네스코어=시군구 압도)
2. **체감 접근성**: 개별시설 좌표로 "가장 가까운 응급실 8분" — 우리만 가능
3. **지표 폭**: SGIS 51지표 전부 커버 + 반려·기후·재난·문화 확장 (§부록 A 패리티)
4. **투명성**: 점수 근거(데이터·가중치·정규화) 드릴다운 → 신뢰가 곧 마케팅
5. **좌표 기반 초개인화**: 직장·학교 주소 → 통근시간 반영 후보지 재랭킹
6. **판정 로직**: 사각지대·결핍 발굴 → B2G(지자체)·언론 데이터저널리즘 시장

### 해자(moat)
읍면동 × 개별시설은 300종을 수집·지오코딩·공간결합해야 가능 — 무거워서 SGIS는 통계로 우회, 동네스코어는 해상도를 포기했다. **그 어려움을 감수하는 것 자체가 진입장벽**.

---

## 2. 공간 단위 & 기준 레이어

- **1차 분석 단위: 읍면동** (인구·정책·부동산 판단의 자연스러운 단위)
- **롤업: 시군구 → 시도** (상위 집계·비교)
- **선택적 정밀 단위: 격자(500m/1km)** — 접근성 정밀 분석용 (2단계 확장)

**기준(base) 레이어 — 반드시 먼저 확보:**
1. **행정구역 경계** (읍면동 폴리곤): `전국법정구역(읍면동)정보표준데이터` 또는 `국가데이터처_SGIS 행정구역 통계 및 경계`
2. **인구·세대·연령 구조** (읍면동별): SGIS 또는 행안부 주민등록인구통계
   - → 모든 밀도 지표의 **분모(1인당·세대당)** 및 페르소나 판정(고령/영유아 비중)에 사용

---

## 3. 지표 체계 (Indicator Framework)

8개 도메인 → 세부지표 → 실제 표준데이터 매핑. `(-)` 는 **부정 지표**(많을수록 감점).

### D1. 의료·건강
- 약국(`전국약국`), 응급의료기관(`전국응급의료기관`), 의료기관(`전국의료기관`), 보건기관(`전국보건기관`)
- AED(`전국자동심장충격기`), 치매센터(`전국치매센터`), 건강증진센터

### D2. 교육·보육
- 어린이집(`전국어린이집`), 유치원(`전국유치원`), 초·중등학교(`전국초중등학교위치`)
- 도서관(`전국도서관`), 학원·교습소(`전국학원및교습소`), 평생교육시설

### D3. 생활편의·상업
- 상권(소상공인 `상가(상권)정보`), 전통시장(`전국전통시장`), 대규모점포(`전국대규모및준대규모점포`)
- 주유소(`전국주유소`), 전기차충전소(`전국전기차충전소`), 무인민원발급(`전국무인민원발급정보`)

### D4. 문화·여가·체육
- 도시공원(`전국도시공원정보`), 녹지(`전국녹지`), 체육시설(`전국체육시설`), 실외운동기구
- 박물관·미술관(`전국박물관미술관정보`), 공연장(`전국공연장`), 영화상영관(`전국영화상영관`)

### D5. 교통·이동
- 버스정류소(`전국버스정류소`), 도시철도역(`전국도시철도역사정보`), 환승센터
- 주차장(`전국주차장정보`), 자전거도로·대여소, 택시승강장

### D6. 안전
- CCTV(`전국CCTV`), 어린이보호구역(`전국어린이보호구역`), 안심귀가·안심택배·안전비상벨
- 교통사고다발지역(`전국교통사고다발지역`) `(-)`, 신호등·횡단보도·과속방지턱

### D7. 환경·기후
- 무더위쉼터·미세먼지쉼터·그늘막쉼터, 대기질측정소, 공중화장실·무료와이파이
- 환경오염위반사업장(`전국환경오염위반사업장`) `(-)`, 폐수배출사업장 `(-)`

### D8. 복지·돌봄
- 사회복지시설(`전국사회복지시설`), 경로당·마을회관, 무료급식소, 지역아동센터
- 장애인편의시설(`전국장애인편의시설`), 교통약자이동지원센터

> 도메인·지표는 확정본이 아니라 **설정 파일(config)** 로 관리 → 추가/제거/재분류가 코드 수정 없이 가능하도록 설계.

---

## 4. 산출 알고리즘

각 읍면동 `u`, 지표 `i` 에 대해 두 신호를 계산한다.

### 4.1 원시 신호 (raw signals)
1. **공급 밀도(supply)**: 인구 1만 명당 시설 수 `= count(i, u) / pop(u) * 10,000`
   - 면적 기반(`/ km²`)은 밀집도 보조지표로 병행
2. **근접성(proximity)**: 인구가중 중심점 → 최근접 시설까지 거리/도보시간
   - 표현: "가장 가까운 응급실까지 8분" — 시민에게 직관적

> **좌표 있는 데이터**는 근접성·밀도 모두 산출. **주소만 있는 데이터**는 법정동 코드로 매핑해 밀도만 산출(근접성 생략) 또는 지오코딩 후 산출.

### 4.2 정규화 (normalization)
- 지표별 **전국 백분위(0~100)** 를 기본 사용 (이상치에 강건).
- 부정 지표는 `100 - 백분위` 로 반전.
- **도농 형평성 보정**: 밀도는 농촌에 불리 → 두 가지 비교 축을 함께 제공.
  - (a) 전국 백분위 (b) **동일 코호트 백분위**(도시/도농복합/농촌 3분류 내 비교)
  - 근접성 지표는 희소지역에서도 의미가 있어 형평성 완충 역할.

### 4.3 집계
```
도메인 점수(u, D) = Σ_i [ w_i · norm(signal_i) ]      (도메인 내 지표 가중평균)
NLI(u)          = Σ_D [ W_D · 도메인점수(u, D) ]       (도메인 가중합)
```
- 결측 지표는 도메인 내 가중치 재정규화(있는 지표끼리만 평균) + **커버리지 플래그** 표기.

---

## 5. 가중치 & 페르소나 (핵심 UX)

주민마다 중요 도메인이 다르다. **프리셋 페르소나 + 사용자 슬라이더**를 제공한다.

| 페르소나 | 강조 도메인 |
|---|---|
| 균등(기본) | 8개 도메인 동일 가중 |
| 영유아 양육 가구 | 교육·보육 ↑, 안전 ↑, 의료 ↑ |
| 고령 가구 | 의료 ↑, 복지·돌봄 ↑, 근접성 가중 ↑ |
| 1인 청년 | 생활편의·상업 ↑, 교통 ↑, 문화·여가 ↑ |
| 반려동물 가구 | 공원·녹지 ↑, 동물병원(확장지표) ↑ |

- 대시보드 슬라이더로 도메인 `W_D` 실시간 조정 → 지도·랭킹 즉시 재계산.
- 기본 가중치는 균등에서 시작, 필요 시 AHP(쌍대비교) 기반 프리셋으로 정교화.

---

## 6. 판정 로직 (Grading & Blind-spot)

1. **등급**: NLI 전국 백분위 밴드 → `S`(상위10%)·`A`·`B`·`C`·`D`(하위10%)
2. **사각지대(blind spot)**: `도메인점수 < 임계치` **AND** `인구 ≥ N` 인 동
   → "많은 주민이 특정 인프라에서 소외" → 정책 우선순위 리스트
3. **결핍 지표(deficiency)**: 인구 있는 동인데 특정 필수시설이 반경 `R` km 내 **0개**
   (예: 응급실·약국·어린이집 부재)
4. **균형성 지표**: 도메인 점수 분산 → 올라운드형 vs 특화형 분류
5. **동일 규모 비교**: 인구·도농 코호트 내 순위 → 공정한 상대평가

---

## 7. 데이터 파이프라인 아키텍처

```
[원천 파일/API]  ← (사용자가 수집·투입)
      │  encoding=UTF-8 통일, 스키마 매핑(config/datasets.yaml)
      ▼
[정규화 레이어]  각 데이터셋 → (이름, 좌표|주소, 도메인, 지표키) 표준 스키마
      │  주소→좌표 지오코딩 / 좌표→법정동 매핑
      ▼
[공간 결합]      포인트 ↔ 읍면동 폴리곤 (point-in-polygon)
      │  + 근접성(최근접거리) 계산
      ▼
[집계 테이블]    unit × indicator (count, 밀도, 최근접거리)
      │
      ▼
[스코어링 엔진]  정규화 → 도메인점수 → 가중합 NLI (가중치 파라미터화)
      │  결과: parquet / DuckDB
      ▼
[대시보드]       지도 + 페르소나 슬라이더 + 검색/비교/드릴다운
```

**기술 스택 제안 (경량·재현 가능):**
- ETL·스코어링: **Python** (표준 `csv` + `duckdb` + `shapely`/`geopandas`)
- 저장: DuckDB 또는 Parquet (단일 파일, 서버 불필요)
- 지도·대시보드: **MapLibre GL(무료 벡터타일) + 웹앱**, 또는 빠른 프로토타입은 **Streamlit**
  - 정적 배포까지 원하면: 스코어를 사전계산 → GeoJSON/PMTiles → 정적 사이트

**디자인 원칙 (경쟁사 대비 개선):**
- **읍면동 코로플레스** 지도(동네스코어 버블맵 대비 정밀·직관)
- 페르소나 **슬라이더 조작 → 지도 즉시 재계산**(SGIS의 무거운 재조회 흐름 개선)
- **점수 카드 + 근거 설명**: "왜 이 점수인지"를 항상 노출(투명성 강점 시각화)
- **나란히 비교 뷰**(최대 N개 동) + 후보지 저장
- **모바일 우선**, 관공서풍 UI 탈피 — 여백·타이포·색 위계 정돈

> 기존 코드베이스가 없으므로 스택은 유동적. 1단계는 Python+DuckDB로 지수 산출을 확정하고, 2단계에서 대시보드 형태를 고른다.

---

## 8. 로드맵 (단계)

**Phase 0 — 기준 확보** *(지금·데이터 수집 단계)*
- 읍면동 경계 + 인구 통합본 확보 → 지오 프레임 구축

**Phase 1 — MVP 지수** (도메인당 대표지표 1~2개, 총 15~20개 표준데이터)
- 정규화·가중합·등급까지 산출, 결과를 표/GeoJSON으로 검증

**Phase 2 — 지도·대시보드**
- 코로플레스 지도 + 페르소나 슬라이더 + 지역 검색·비교

**Phase 3 — 심화**
- 근접성(도보시간) 정밀화, 격자 단위, 사각지대 리포트 자동 생성, 시계열(연도별 변화)

---

## 9. 열린 결정 사항 (진행 중 확정)

- 인구 정규화 기준연도·출처(SGIS vs 행안부) — 수집 데이터 확인 후 확정
- 근접성 계산: 직선거리(빠름) vs 도로망 도보시간(정확, 라우팅 필요) — MVP는 직선거리
- 대시보드 형태: Streamlit(속도) vs MapLibre 웹앱(완성도) — Phase 2 착수 시 선택
- 지표별 도메인 내 가중치 `w_i`: 초기 균등 → 사용 피드백으로 조정

---

*다음 액션: `데이터수집목록.md`의 우선순위에 따라 기준 레이어부터 수집 → 투입되면 Phase 1 스코어링 엔진 착수.*

---

## 부록 A — SGIS 51지표 패리티 매핑

"데이터 ≥ 공공"을 보증하려면 SGIS 7카테고리 51지표를 **전부 대응**해야 한다. 지표는 성격이 둘로 나뉜다:
`[P]` = 개별시설 **포인트**(우리 강점, 좌표로 접근성까지) / `[S]` = 비율·**통계**(별도 통계 데이터 필요).

| SGIS 카테고리 | 51지표 | 우리 소스 | 유형 |
|---|---|---|---|
| **자연** | 대기질(미세먼지/일산화탄소) | 대기질측정소 | P/S |
| | 여름·겨울 체감온도 | 기상청 통계 | S |
| | 녹지비율, 1인당 도시공원 면적 | 전국녹지·도시공원(면적) | P |
| **주택** | 아파트/연립다세대/단독 비율, 주거면적, 노후주택비율, 자가점유율 | **인구주택총조사 통계** | S |
| | 공시지가, 면적당 아파트가격 | 전국개별공시지가·공동주택 표준데이터 | P |
| **지역인구** | 유소년/청장년/고령 비율, 1인가구, 사업체종사자, 순유입 | **주민등록·인구총조사·사업체 통계** | S |
| **안전** | 화재/범죄/생활/감염병 안전 | **소방청·경찰청·질병청 통계** | S |
| | 교통사고 안전, CCTV 분포 | 전국교통사고다발지역·CCTV 표준데이터 | P |
| **생활편의교통** | 편의/백화점/마트/슈퍼편의점/식료품/외식 수 | 소상공인 상권정보·대규모점포 | P |
| | 대중교통 시설 수 | 전국버스정류소·도시철도역사 | P |
| | 반려동물 서비스업 수 | 전국동물병원·동물판매업 등 | P |
| **교육** | 초·중·고 학교 수, 학원 수 | 전국초중등학교위치·학원교습소 | P |
| | 초·중·고 학급당 학생 수 | **교육통계(학급/학생)** | S |
| **복지문화** | 유치원·보육/병원/의원/약국/동물병원/노인·사회·장애인복지/문화/체육시설 수 | 해당 표준데이터 다수 | P |

- `[P]` 지표는 **우리가 우위** — 개수뿐 아니라 최근접 거리까지 산출.
- `[S]` 지표(주택·인구구성·안전등급·학급당 학생수)는 통계청/소방청/경찰청/교육통계 별도 수집 필요 → `데이터수집목록.md` 우선순위 1.5에 추가.
- **결론**: `[P]` 다수 + `[S]` 확보 시 51지표 완전 커버 + 접근성·해상도로 초과 달성.
